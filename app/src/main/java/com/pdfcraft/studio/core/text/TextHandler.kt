package com.pdfcraft.studio.core.text

/**
 * Contract for adding and styling text blocks on a PDF page (font, size,
 * color, alignment). Left as a stub for this stage.
 */
interface TextHandler {
    // fun addTextBlock(page: Int, content: String, style: TextStyleModel): TextBlockId
}
