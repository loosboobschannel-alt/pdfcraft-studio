package com.pdfcraft.studio.core.settings

/**
 * Contract for reading/writing user-facing app settings (default page
 * size, theme preference, export quality, etc.). Left as a stub for this
 * stage — no settings screen exists yet.
 */
interface AppPreferences {
    // fun getDefaultPageSize(): PageSize
    // fun setDefaultPageSize(size: PageSize)
}
