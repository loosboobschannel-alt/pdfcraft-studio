package com.pdfcraft.studio.core.link

/**
 * Contract for attaching clickable hyperlinks/annotations to regions of a
 * PDF page. Left as a stub for this stage.
 */
interface LinkHandler {
    // fun addLink(page: Int, bounds: RectModel, url: String)
}
