package com.pdfcraft.studio.core.image

/**
 * Contract for importing, placing, and transforming images inside a PDF
 * page (pick from gallery/camera, crop, resize, position). Left as a stub
 * for this stage — the editor canvas will call into this once image tools
 * are implemented.
 */
interface ImageHandler {
    // fun importImage(uri: android.net.Uri): Result<ImageAsset>
}
